language: cpp

dist: xenial
sudo: false

git:
  submodules: false


before_script:
  - git submodule update --init --depth=1


apt:
  packages:
    - g++-5
    - python3.5
    - python3.5-dev
    - ninja-build


script:
  - ninja all test
