sudo: false

language: cpp

os: linux
dist: xenial

git:
  submodules: false


before_script:
  - git submodule update --init --depth=1


apt:
  packages:
    - g++-5
    - python3.5
    - python3.5-dev
    - ninja-build


script:
  - mkdir build
  - (cd build ; cmake -G Ninja .. ; ninja all test)
